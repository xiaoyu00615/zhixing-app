# 04｜测试与验收规范

版本：V1.1  
日期：2026-09-22
状态：CURRENT

## 1. 目标

测试不是证明“代码看起来能跑”，而是确认：

- 功能真的可执行；
- 数据真的持久化；
- 重启后不会丢；
- Error / Empty / Loading 可恢复；
- Web 与 Native 行为符合统一 Contract；
- UI 符合冻结规范；
- 新修改没有破坏已验收模块；
- 高风险数据操作不会静默丢数据。

## 2. 测试状态

只允许：

- PASS；
- FAIL；
- NOT RUN；
- BLOCKED；
- NOT APPLICABLE。

未运行不得标 PASS。

## 3. Level 1｜Unit Test

主要工具：Vitest。

适合：

- Overdue Calculation；
- Importance / Urgency / Quadrant 规则；
- Date Helper；
- Entity Validation；
- Node Content Schema；
- Search Query Builder；
- Manifest Validation；
- Conflict Decision Logic 的纯函数部分。

## 4. Level 2｜Repository / Database Integration

必须覆盖：

- Migration；
- CRUD；
- Transaction；
- Soft Delete；
- Restore；
- archived_at；
- Relation；
- FTS；
- Restart Persistence；
- Web / Native Repository Contract 一致性。

典型测试：

```text
Create
→ Commit
→ Close DB
→ Reopen
→ Query
→ Data still exists
```

## 5. Level 3｜Component / UI Test

覆盖：

- Buttons / Inputs；
- Modal / Drawer；
- Form Validation；
- Empty / Loading / Error；
- Task Row；
- Canvas Toolbar / Node Property；
- Editor Save State；
- Search Result Preview；
- Backup / Conflict 高风险确认组件。

## 6. Level 4｜Web E2E

主要工具：Playwright。

覆盖完整用户流：

```text
Create
→ Edit
→ Refresh
→ Reopen
→ Delete
→ Restore
```

Phase 1B 后必须对 Web 真实 OPFS 持久化做 E2E，不以 Memory Mock 代替主验收。

## 7. Level 5｜Native Smoke / Integration

### Windows

- Tauri 启动；
- SQLite；
- 文件系统；
- Attachment；
- StorageAdapter；
- Backup / Restore；
- 数据位置迁移；
- Notification（涉及时）；
- Sync（Phase 7）。

### Android

- 启动；
- 本地数据；
- 触控；
- 返回行为；
- 软键盘；
- 文件；
- 局域网 Sync（Phase 7）。

## 8. UI 验收基准

### 1920 × 1080

- Sidebar 224 px；
- Topbar 60 px；
- 主内容不异常拉伸；
- 首页 KPI 四列；
- Drawer 不溢出；
- Modal 正确居中；
- Canvas 可用区域最大化；
- Settings 二级导航与内容宽度正常。

### 1440 × 900

- 首页 KPI 仍优先四列；
- 无异常横向滚动；
- Sidebar 不压缩错误；
- 顶部工具不重叠；
- Drawer 可用；
- 主操作仍可见。

### 1280 px Desktop

核心页面仍可完成主要任务。更窄断点必须实际验证后再设计，不可由 Agent 猜测。

## 9. 系统状态验收

### Empty

有明确标题、说明、单一主要行动。

### Loading

优先 Skeleton；局部操作才用 Spinner。

### Error

必须解释问题并提供重试 / 恢复路径。

### Offline

核心本地功能继续工作。主页面不长期显示大型 Sync 状态。

### Local Save

编辑类页面至少考虑：

- 正在保存；
- 已保存到本地；
- 保存失败 / 重试。

## 10. Phase 1A 验收

至少验证：

1. Web dev server 启动；
2. Windows Tauri 启动；
3. Router / Sidebar / Topbar；
4. Design Token；
5. Native SQLite 创建；
6. Migration 执行；
7. Repository 写入；
8. 应用关闭后重启数据仍存在；
9. Error / Logger 基础可用；
10. Test DB 可注入；
11. 1920 / 1440 UI 验证；
12. 无关键 Console / Rust error。

## 11. Phase 1B 验收

至少验证：

1. SQLite WASM Worker 启动；
2. OPFS Capability Detection；
3. Web Repository 与 Native Repository Contract 一致；
4. 浏览器刷新后数据存在；
5. 关闭 / 重开页面后数据存在；
6. OPFS 不可用时出现明确提示，不静默回退丢数据；
7. Web Integration / E2E PASS。

## 12. Task 验收

必须验证：

- 创建 / 编辑；
- todo / doing / completed / cancelled；
- Deadline；
- Overdue；
- Importance；
- Urgency；
- Quadrant；
- Project nullable；
- Tag；
- Soft Delete；
- Trash Restore；
- Web / Native 重启持久化；
- 1000 条数据基础性能。

特别验证：Overdue 不自动修改 Importance。

## 13. Canvas 验收

至少：

- 新建 Canvas；
- text / sticky / image / link / task / note / diary Node；
- Drag；
- Zoom / Pan；
- Edge；
- Selection / Multi-select；
- Group / Ungroup；
- Copy / Paste；
- Undo / Redo；
- 保存坐标；
- 重启恢复；
- Linked Entity 软删除后 Node 保留并显示失效；
- 当前画布 Search；
- 100 Node 基础流畅性。

Undo / Redo 不要求跨重启保存整个操作历史。

## 14. Document / Diary / Note 验收

至少：

- Note 创建；
- Diary 按 `diary_date` 创建 / 补写；
- Tiptap 内容保存；
- plain_text 同步；
- Local Auto Save；
- Save Failure；
- Tag Relation；
- Attachment；
- Note Folder；
- Favorite；
- 重启恢复；
- 10000 字文档基础性能。

## 15. Search / Archive / Trash 验收

### Search

Global Search V1（Phase 5，已实现并通过真实 Web E2E）：

- Task（title）、Note（title + content）、Diary（title + content）、Canvas（title）可搜；
- 短查询回退：<3 字符 → 转义 LIKE；>=3 字符 → trigram FTS MATCH，多词 AND；
- 跨域统一结果流；
- Trash / 软删除实体不出现（active-only）；
- Preview（实体标签 + 标题回退 + 纯文本摘要）→ Open Entity（`?id=` 深链 / `/canvas/<id>`）。

V1 未实现（验收不得视为已完成）：

- Canvas 节点文本检索；
- Tag / Archive / Trash 检索；
- 结构化筛选与 facet、搜索历史、自动补全、语义 / 向量检索。

真实 Web E2E 覆盖（`tests/e2e/search-persistence.spec.ts`，Playwright + 隔离浏览器 profile）：

- 真实 Web sqlite-wasm + OPFS + migration 0013，不使用 Search mock；
- 通过真实产品流创建 Task / Note / Diary / Canvas 后跨域检索；
- Note / Diary 正文检索；
- 短查询 LIKE 回退；
- reload 后（同一隔离 profile）索引内容仍可检索；
- 结果打开并校验可见实体身份；
- 软删除后不再出现在 Search。

### Tags V1（Task-only，CLOSED）

已实现能力（CLOSED）：

- Tag 实体：create / list / rename（Native + Web 对称实现）；
- Task 关系：assign（添加标签）/ remove（移除标签）；
- Task Filter：按单标签过滤列表（与 project filter 可组合）；
- 持久化：Native（Rust / SQLite）+ Web（共享 Worker / sqlite-wasm / OPFS），reload 后标签与关系存活；
- Native / Web repository parity：focused 测试覆盖。

浏览器证据（必须准确，禁止写成 "Tag E2E PASS"）：

- `tests/e2e/tag-persistence.spec.ts`（1 用例，隔离 profile + 真实 UI → service → repository → 共享 Worker → sqlite-wasm → OPFS）；
- 业务断言：create Tag → create Task → assign → filter → reload 持久化 → rename（关系跟随）→ remove relation（实体保留）→ final reload 全部完成；
- 形式化 Playwright runner 结果：BLOCKED by local post-test worker teardown（两次 clean attempt 均在产品断言全部完成后卡在 worker 退出阶段）；
- 产品断言失败：NONE。

### Playwright 本地环境 watch（NON-BLOCKING / TEST-INFRASTRUCTURE）

在当前 Windows 验证环境下，persistent-context 的 Tag E2E 能完整跑完产品断言，但 Playwright worker 在测试结束后常无法退出（被 force-kill）。这**不**归类为 Tag 产品失败。本 closeout 不提交任何 process-kill 变通代码；如未来需要形式化 E2E PASS，在测试基础设施层面单独处理（与 Tag 产品解耦）。

### Archive（P5C Archive V1，已实现并通过真实 Web E2E）

实体范围：Task + Note。Diary / Canvas 不在 V1 范围。

已验收：

- Task → Archive；
- Note → Archive；
- Unarchive（Task / Note）；
- Archive → Trash（不允许从 Archive 直接物理永久删除）；
- Trash Restore → Archive（保留 archived 状态，回到 Archive 工作区，仍不出现在普通 Search）；
- 普通 Global Search 排除 archived Task / Note；
- Unarchive 恢复 Search 投影包含；
- Note archive 前 autosave；
- Web / Native runtime 一致；ArchivePage。

V1 未实现（验收不得视为已完成）：

- Permanent Delete from Archive；
- Clear Archive；
- Bulk Archive / Bulk Unarchive；
- Archive Search；
- Diary Archive；
- Canvas Archive。

真实 Web E2E 覆盖：

- `tests/e2e/archive-persistence.spec.ts`（1 用例，PASS）：Task archive → reload 持久化；Task unarchive；Note archive → Trash → Restore → Archive（保留 archived 状态）；最终 Note unarchive，最新内容 reload 后仍保留；
- `tests/e2e/search-archive-lifecycle.spec.ts`（S4 新增，1 用例，PASS）：Active 可检索、Archive 排除、Archive→Trash 排除、Trash Restore→Archive 排除、Unarchive 重新纳入。

以上均为真实浏览器 E2E（Playwright + 隔离 OPFS profile），非 mock。

### Trash（P5B Unified Trash V1，已实现并通过真实 Web E2E）

实体范围：Task、Note、Diary。Canvas / Canvas Node / Canvas Edge 明确不在 V1 范围内。

已验收：

- 跨域统一列表（仅软删除实体）；
- 单项 Restore，且只走 canonical 源域服务（`TaskService.restoreTask` / `NoteService.restore` / `DiaryService.restore`）；
- 真实 Web 持久化（既有共享 Worker + sqlite-wasm + 隔离 OPFS）；
- reload 后删除态与恢复态均存活；
- 软删除期间不出现在 Search（active-only）；
- canonical restore 后重新出现在 Search；
- 空标题 display-only 兜底（Note→`未命名笔记`、Diary→`未命名日记`）。

V1 未实现（验收不得视为已完成，也不得在 Trash 工作区内出现对应控件）：

- Permanent Delete 强确认；
- Clear Trash 强确认；
- Bulk Restore / 多选；
- Trash 内筛选 / 检索；
- Canvas Trash；
- Archive 集成。

未确认时不自动物理清理核心数据，这一条继续有效。

Unified Trash V1 状态：CLOSED（P5B S4 已验证，当前 P5C-S4 slice 完成最终 docs closeout）。

真实 Web E2E 覆盖（`tests/e2e/trash-persistence.spec.ts`，Playwright + 隔离持久浏览器 profile）：

- 通过真实产品流创建 Task / Note / Diary，不 mock TrashRepository / TrashService / 域 restore 服务 / Search；
- 真实 Web sqlite-wasm + OPFS；
- 通过各自正常产品 UI 软删除（Task 详情 `移入回收站`；Note / Diary 编辑器 `删除` → `确认删除`）；
- 删除后源工作区与 Search 均不再出现；
- `/trash` 三个实体行出现；reload 后仍存在（关键持久化证明）；
- 逐个恢复后源域重新出现，Diary 原始日期身份不变；
- 全部恢复后为空态；
- 恢复后 Search 重新纳入；reload 后恢复态仍为持久化事实。

### P5C-S4 最终回归（真实执行事实，非 rerun）

- cargo check：PASS；
- cargo test：`213 passed / 0 failed`；
- Vitest：`1319 passed / 0 failed / 82 files`；
- typecheck、lint、build、build:e2e：PASS；
- `git diff --check`：PASS；
- Affected E2E regression（archive-persistence / search-persistence / trash-persistence / search-archive-lifecycle）：PASS（5 tests / 3.8m / exit 0）；
- 全量 Playwright：NOT RUN（共享 Web persistence 无变更）；
- S2.5 Search × Archive browser-level lifecycle watch：RESOLVED。

只有真实执行过的才标记为 PASS；未运行项明确标 NOT RUN。

## 16. Storage / Migration 验收

至少：

- 简单模式路径；
- 高级模式路径；
- 目标空间不足；
- 目标不可写；
- 数据位置突然失效；
- 不创建空数据库；
- Pause + Flush；
- Safety Snapshot；
- Temporary Target；
- Hash / `integrity_check`；
- Reopen；
- Switch；
- 旧数据未被自动删除。

需要模拟迁移中断，验证原数据仍可恢复。

## 17. Backup / Restore 验收

至少：

- Manual Backup；
- Auto Backup；
- Backup History；
- Portable Settings；
- Backup Manifest；
- 文件 Hash；
- Staging Restore；
- Restore 前 Safety Snapshot；
- Backup 损坏时拒绝切换；
- Attachment 缺失时明确失败 / 警告；
- Device-local Settings 不被跨设备 Backup 覆盖。

## 18. LAN Sync 验收

### Case A｜创建

PC 创建 Task A → Sync → Android 出现 A。

### Case B｜修改

Android 修改 A → Sync → PC 收到修改。

### Case C｜离线分叉

两端离线各自新增 → 恢复局域网 → 两端最终都有完整变化。

### Case D｜冲突

两端同时修改同一 Entity → 不得静默 Last Write Wins → 进入 Conflict。

### Case E｜删除

PC Soft Delete → Sync → Android 进入对应删除状态 → 后续不得无依据复活。

### Case F｜断点续传

同步中断 → 重连 → 从正确 Cursor 继续。

### Case G｜Attachment

不同设备本地路径不同，但通过 Attachment metadata + hash + bytes 正确传输，并由目标 StorageAdapter 决定保存位置。

### Case H｜Remove Device

撤销 Trust 后停止同步，但另一设备本地业务数据不被远程删除。

## 19. 测试报告格式

```text
测试名称：
执行命令：
执行环境：
执行时间：
结果：PASS / FAIL / NOT RUN / BLOCKED / N/A

失败日志：
原因：
修复：
重测结果：
```

重要 Phase 测试记录保存到 `/docs/testing/`。

## 20. Definition of Done

一个模块只有同时满足以下条件才可 DONE：

- UI 完成；
- 业务逻辑完成；
- Repository / 数据层完成；
- 数据重启恢复；
- Error / Empty / Loading 完成；
- 适用自动测试 PASS；
- Web Build PASS；
- 适用 Native Smoke PASS；
- 响应验证 PASS；
- 用户人工审核通过。

“页面能打开”不等于 DONE。
